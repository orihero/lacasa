import { describe, expect, it } from 'vitest';

// Placeholder — proves the vitest + @lacasa/config-vitest wiring works
// end to end for this workspace. Replace/extend with real route and
// service tests as they land.
describe('apps/api test wiring', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
