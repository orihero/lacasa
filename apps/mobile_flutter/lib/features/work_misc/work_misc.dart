/// Barrel for `lib/features/work_misc/` — SCREENS.md §21 (`connected-accounts`),
/// §22 (`notifications`), §23 (`messages`). Exports only what
/// `app_router.dart` needs, same convention as `features/agents/agents.dart`.
library;

export 'widgets/connected_accounts_screen.dart';
export 'widgets/messages_screen.dart';
export 'widgets/notifications_screen.dart';
