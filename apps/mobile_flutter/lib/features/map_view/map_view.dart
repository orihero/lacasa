/// Barrel for `lib/features/map_view/` — SCREENS.md §3.6. Exports only what
/// `app_router.dart` needs, same convention as `features/home/home.dart`.
library;

export 'map_view_args.dart';
export 'widgets/map_view_screen.dart';
