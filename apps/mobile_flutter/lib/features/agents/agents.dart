/// Barrel for `lib/features/agents/` — the two agent screens
/// (`agents-directory` §3.9 and `agent-profile` §3.10), which share one
/// repository and one fixture set. Exports only what `app_router.dart`
/// needs, same convention as `features/home/home.dart`.
library;

export 'widgets/agent_profile_screen.dart';
export 'widgets/agents_directory_screen.dart';
