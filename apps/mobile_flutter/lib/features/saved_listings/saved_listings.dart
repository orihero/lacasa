/// Barrel for `lib/features/saved_listings/` — `saved-listings` (SCREENS.md
/// §3.17). Exports only what `app_router.dart` needs, same convention as
/// `features/agents/agents.dart`.
library;

export 'widgets/saved_listings_screen.dart';
