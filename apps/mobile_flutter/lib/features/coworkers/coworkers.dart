/// Barrel for `lib/features/coworkers/` — SCREENS.md §35 (`coworkers-list`),
/// §36 (`coworker-detail`), §37 (`add-coworker`). Exports only the three
/// root screen widgets `app_router.dart`'s integration pass wires up, per
/// this app's convention (see `features/agents/agents.dart`,
/// `features/saved_listings/saved_listings.dart`).
library;

export 'widgets/add_coworker_screen.dart';
export 'widgets/coworker_detail_screen.dart';
export 'widgets/coworkers_list_screen.dart';
